function [x]=bissecao(f,a,b,N)
for i=1:N
    if f(a)*f(b)<0
        x0=(a+b)/2;
    if f(x0)*f(a)<0
         b=x0;
    else 
         %f(x0)*f(b)<0    
         a=x0;
    end 
    end
    i=i+1;
    x=x0;
end